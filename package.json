{
  "name": "ai-atan-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run build && node scripts/serve.mjs dist",
    "build": "npm run clean && tsc && node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "lint": "tsc --noEmit",
    "test": "tsc && node tests/router.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
